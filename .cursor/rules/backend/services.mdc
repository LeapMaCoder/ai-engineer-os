---
description: services/ 后端实现约束
globs: services/**/*
alwaysApply: false
---

# 后端规则

## 职责

在已批准的 Architecture 下实现已批准的 Specification。

## 前置条件

写服务代码前确认：

- Spec 存在且为最新
- Architecture（必要时含 ADR）存在
- Task 引用 Spec ID

## 实践

- 偏好简单局部逻辑，避免过早抽象
- 密钥不进源码；使用环境变量 / 密钥管理
- 行为或设计变更时更新 Spec/Architecture
- 仅在真实复用出现时，把共享契约放入 `packages/`

## 禁止

- 实现未规格化的接口/行为
- 无 ADR 引入新的重大依赖
- 业务逻辑放在 `scripts/` 或 `infrastructure/`
