---
description: LeapMa 全局 SDD 原则与 Monorepo 边界
alwaysApply: true
---

# LeapMa 全局规则

## 身份

LeapMa 是 AI Native 程序员成长平台（不是传统课程网站）。

## Source of Truth

`docs/` 是权威来源：

Vision → Product → Specification → Architecture → Code → Test

若代码与文档冲突，先按 SDD 更新文档（或改代码以符合文档）。

## 硬性禁止

- 没有 Product + Specification，禁止实现功能。
- 没有 Architecture + ADR，禁止擅自定技术栈 / 云厂商。
- 在纯流程/文档任务中，禁止添加业务代码。
- 禁止为假想复用创建 Util/Helper/Manager 层（三次原则）。
- 禁止扩大到请求范围之外。

## Monorepo 边界

| 路径 | 职责 |
|------|------|
| `docs/` | 产品与工程真理 |
| `apps/` | 面向用户的应用 |
| `services/` | 后端与 AI 服务 |
| `packages/` | 共享库 |
| `infrastructure/` | 部署 / 云 / Docker |
| `tests/` | 跨切面测试 |
| `scripts/` | 仓库脚本 |
| `.cursor/` | AI 规则 |

## 协作

为未来人类与 AI Agent 写作：清晰 README、链接上游文档、偏好小 diff。
